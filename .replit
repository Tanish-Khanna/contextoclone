modules = ["python-3.12", "web"]
run = "gunicorn app:app --bind 0.0.0.0:8080 --workers 4"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "gunicorn app:app --bind 0.0.0.0:8080 --workers 4"]
deploymentTarget = "cloudrun"
